const string = "Spilled Coffee Remains Nation's Leading Cybersecurity Threat https://downtime.dev#50034"

const encoded = encodeURIComponent(string)

console.log(encoded)


// function getRandomInt(min, max) {
//   min = Math.ceil(min);
//   max = Math.floor(max);
//   return Math.floor(Math.random() * (max - min) + min); //The maximum is exclusive and the minimum is inclusive
// }

// function getInt() {
  
//   return Math.floor(Math.random() * 90000 + 10000); //The maximum is exclusive and the minimum is inclusive
// }

// console.log(getInt())